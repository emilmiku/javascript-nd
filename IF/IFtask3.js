// ## 3 Write a JavaScript function `compareNumbers` that accept two integers and display the larger.
// Use conditional statement `if` and conditional (ternary) operator `?`.

"use strict";

const prompt = require("prompt-sync")();
